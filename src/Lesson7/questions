1. Что за утилитарный класс?
2. Можно ли создать объект класса по имени класса getClass
3. Как работать с Cloneable
4. Не могу запомнить операции += и =+ чем отличаются... Всегда методом тыка решаю подобные задачи

Урок 8
1. Почему в некоторых случаях не срабатывает конструктор по заполнения программного кода equals b hashCod
2. Для чего, в процедуре в которой будет запущен процесс клонирования нужно прописывать throws CloneNotSupportedException
3. Ещё раз спросить про строки, как их лучше добавлять? Конкатенацией лучше не пользоваться?
    sb.append(item.value.toString()).append(", "); будет возвращаться новая строка по "item.value.toString()"
    зачем нужен compareTo
4. Спросить как шагать до следующей точки останова
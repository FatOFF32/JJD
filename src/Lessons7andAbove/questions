1. Что за утилитарный класс?
2. Можно ли создать объект класса по имени класса getClass
3. Как работать с Cloneable
4. Не могу запомнить операции += и =+ чем отличаются... Всегда методом тыка решаю подобные задачи

Урок 8
1. Почему в некоторых случаях не срабатывает конструктор по заполнения программного кода equals b hashCod
2. Для чего, в процедуре в которой будет запущен процесс клонирования нужно прописывать throws CloneNotSupportedException
3. Ещё раз спросить про строки, как их лучше добавлять? Конкатенацией лучше не пользоваться?
    sb.append(item.value.toString()).append(", "); будет возвращаться новая строка по "item.value.toString()"
    зачем нужен compareTo
4. Спросить как шагать до следующей точки останова

Урок 9.
1. Из анонимного класса можно обращаться к переменным метода из которого он был вызван, но не может менять менять эти переменные
    Для чего вообщем сделана возможность обращаться к переменным этого метода?

Урок 10.
1. Зачем нам нужен <> в конце. Зачем для линк листа создавать джинерик.

Урок 11.
1. Откуда брать всякие разные описания встроенных функций? Например .replaceAll("\\p{Punct}", " ") или .split("\\s");
2. Что означениет "Синхронизирован" у коллекций, домустим HashMap или TreeMap
3. Почему связка (Integer) (o1.getValue()).compareTo не работает?
4. Каким способом решать 3 и 4 задачу?
5. Зачем писать статик перед переменной?
6. Почем не работает Clone у arrayList? Пишет что он возвращшает объект.
7. Почему не работает конкотинация для класса Интеджер? curChar++ Хотя в отладке все хорошо.
8. ещё раз спросить про описание форматов строк на русском
9. Задача 6. Куда воткнуть process и cancel
10. Что за HashTable, почему мы ей не пользуемся? Это как ТЗ?
11. Что такое сбалансированное красно-черное дерево?
12. Что такое чар? Я думал это некое число, но по факту чар это закодированный символ?
13. HashSet всегда отсортирован? Он же не имплементирует интерфейс SortedSet
14. Если создавать допустим LinkedHashSet на основании ArrayList (new LinkedHashSet<>(arr);),
    то порядок элементов в нем останется тот же?
15. Если возвращать из функции новый объект с джинериком, и в нем указать тип, то идея говорит что я дурак.
    Пример: return new ArrayList<Message>(lhs);
16. Спросить у ребят, как они делали удаление сообщений по приоритетам? Тупо в цикле? Tasks1
-- Не сделал задачи 4